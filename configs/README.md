# 配置文件

此目录包含WAF的所有配置文件：

## 配置类型

- **server.yaml**：主服务器配置
- **admin.yaml**：管理后台配置
- **rules.yaml**：安全规则配置
- **ai.yaml**：AI模块配置
- **logging.yaml**：日志系统配置
- **metrics.yaml**：监控指标配置
- **storage.yaml**：存储系统配置

## 环境变量

生产环境中，敏感配置可以通过环境变量覆盖配置文件中的值。

## 配置加载顺序

1. 默认配置
2. 配置文件
3. 环境变量
4. 命令行参数